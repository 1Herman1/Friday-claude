# Проект: <Название> (<домен>)

Специфика проекта. Универсальные правила, агенты и воркфлоу — в `CLAUDE.md` и
`docs/core/`. Здесь — только то, что относится именно к этому проекту.

## Рабочая ветка

Ветка: `<branch>`
Репозиторий: `<owner>/<repo>`

Правила ветвления — по `CLAUDE.md`. Не создавать новые ветки без явного запроса.

## Пользователь

Имя: <имя>

Опыт: <что умеет, чего не знает>

Как общаться:
- <например: объяснять код простыми словами, расшифровывать термины>
- <аналогии, которые заходят>

## Технологический стек (реальный)

Важно для агента: указать, чем стек ОТЛИЧАЕТСЯ от дефолта в `docs/core/stack.md`,
и что для этого проекта **не применимо** (например: «не Next.js — не предлагать
`app/api/**/route.ts`, NextAuth, Vercel»).

### Frontend
- Framework:
- Language:
- Styling:
- Routing / State / Forms:

### Backend
- Runtime / API:
- ORM / Validation / Auth:

### База данных и хранилище
- Primary:
- Storage / Cache:

### Инфраструктура
- Hosting / Process manager / Reverse proxy:
- Деплой:

## Правовой контур (для агента `rkn-compliance`)

Универсальные требования — в `docs/core/rkn-compliance.md`. Здесь только факты,
которые нельзя вывести из кода.

**Незаполненное поле агент считает находкой уровня ВАЖНО, а не «проблем нет».**
Прочерк ставится осознанно, с объяснением.

- Оператор ПД: <ИП/ООО, ИНН, ОГРН(ИП), юридический адрес>
- Контакт по вопросам ПД: <email или телефон, публикуется в политике>
- Ответственный за организацию обработки: <ФИО или «не назначен»>
- Уведомление в РКН: <подано ДД.ММ.ГГГГ, рег. № … / не подано / не требуется — почему>
- Карточка в реестре операторов: <ссылка или «нет»>
- Хостинг: <провайдер, страна>
- Физическое расположение БД: <страна, город, ЦОД>
- Бэкапы: <где хранятся, страна, срок хранения>
- Трансграничная передача: <нет / да — перечислить сервисы и страны>
- Сервисы, обрабатывающие ПД: <сервис → какие данные>
- Категории собираемых ПД: <ФИО, телефон, email, адрес доставки, …>
- Сроки хранения по категориям: <категория → срок → что происходит после>
- Способ авторизации: <номер РФ / ЕСИА / email+пароль / иное>
- Cookie и аналитика: <какие счётчики; есть ли механизм согласия>
- Интернет-реклама: <ведётся ли; есть ли erid и отчётность через ОРД>
- Рекомендательные технологии: <есть / нет; опубликованы ли правила>
- Отраслевые и возрастные ограничения: <например, ветпрепараты, 18+>
- Дата последней проверки комплаенса: <ДД.ММ.ГГГГ, кем>

## Домены проекта (бизнес-логика)

- <ключевые правила: единицы измерения, инварианты, лимиты>

Подробные бизнес-процессы — в `business-processes.md`.

## Старт каждого нового чата

1. Прочитать этот файл и `CLAUDE.md`
2. Поприветствовать пользователя
3. Напомнить рабочую ветку и репозиторий

## Git

- Коммитить только по явной просьбе пользователя
- Пушить на указанную выше ветку
- Сообщения коммитов: короткие, на английском, «что и зачем»
- Не создавать Pull Request без явной просьбы
