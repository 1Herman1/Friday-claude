-- Отметка, начиная с которой выданные токены считаются действительными.
-- Токен живёт неделю, поэтому смена почты, блокировка и обезличивание должны
-- гасить ранее выданные JWT сразу, а не ждать истечения срока.
--
-- Колонка nullable и без DEFAULT: у существующих пользователей отметки нет,
-- значит выкатка не выбрасывает из аккаунтов тех, кто сейчас на сайте.
ALTER TABLE "users" ADD COLUMN "sessionsValidFrom" TIMESTAMP(3);
